<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.hcnc.mapper.HcncMapper">
	<select id="selectTest" resultType="String">
		SELECT VERSION()
	</select>
	
	<insert id="insertTestData" parameterType="Map">
	INSERT INTO HCNC.HTML_P1 (P1,P2,P3)
	VALUES(#{P1}, #{P2}, #{P3})
	</insert>

	<select id="selectBoard" parameterType="Map" resultType="Map">
	SELECT BOARD_NO
		, TITLE
		, CONT
		, WRITER
		, MODIFIER
		, REG_DT
		, MOD_DT
	FROM HCNC.HCNC_BOARD
	WHERE 1=1
		<if test='boardNo != null and boardNo != ""'>
		AND BOARD_NO = #{boardNo}
		</if>
	</select>
	<select id="selectBoardList" parameterType="Map">
	SELECT BOARD_NO
		, TITLE
		, CONT
		, WRITER
		, MODIFIER
		, REG_DT
		, MOD_DT
	FROM HCNC.HCNC_BOARD
	WHERE 1=1
	</select>
	<select id="selectBoardDetail" parameterType="Map">
	SELECT BOARD_NO
		, TITLE
		, CONT
		, WRITER
		, MODIFIER
		, REG_DT
		, MOD_DT
	FROM HCNC.HCNC_BOARD
	WHERE 1=1
	  AND BOARD_NO = #{boardNo}
	</select>
	
	<insert id="insertBoard" parameterType="Map">
		INSERT INTO HCNC.HCNC_BOARD(TITLE, CONT, WRITER, MODIFIER, REG_DT, MOD_DT)
		VALUES (#{TITLE}, #{CONT}, #{WRITER}, #{MODIFIER}, CURRENT_TIMESTAMP(), CURRENT_TIMESTAMP())
	</insert>
	
	<update id="updateBoard" parameterType="Map">
		UPDATE HCNC.HCNC_BOARD
		   SET TITLE = #{TITLE}
		   	, CONT = #{CONT}
		   	, MODIFIER = #{MODIFIER}
		   	, MOD_DT = CURRENT_TIMESTAMP()
	   	WHERE BOARD_NO = #{BOARD_NO}
	</update>
	
	<delete id="deleteBoard" parameterType="Map">
		DELETE FROM HCNC.HCNC_BOARD WHERE BOARD_NO = #{BOARD_NO}
	</delete>
</mapper>