<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.hcnc.mapper.HcncMapper">

	<select id="selectBoardList" parameterType="Map" resultType="Map">
		SELECT BOARD_NO	  as boardNo
			 , TITLE	  as title
			 , CONT		  as cont
			 , WRITER	  as writer
			 , MODIFIER	  as modifier
			 , REG_DT	  as regDt
			 , MOD_DT	  as modDt
			 , DEL_YN	  as delYn
			 , BOARD_TYPE as boardType
			 , CATEGORY	  as category
		FROM hcnc2.HCNC_BOARD
		WHERE DEL_YN = 'N'
	</select>
	
	<insert id="insertBoard" parameterType="Map">
		INSERT INTO hcnc2.HCNC_BOARD (
			TITLE
			, CONT
			, WRITER
			, MODIFIER
			, BOARD_TYPE
			, CATEGORY
			, REG_DT
			, MOD_DT
		)
		VALUES (
			#{title}
			, #{cont}
			, #{writer}
			, #{modifier}
			, #{boardType}
			, #{category}
			, CURRENT_TIMESTAMP()
			, CURRENT_TIMESTAMP()
		)
	</insert>
	
	<update id="updateBoard" parameterType="Map">
		UPDATE hcnc2.HCNC_BOARD
		SET TITLE 	   = #{title}
		  , CONT 	   = #{cont}
		  , MODIFIER   = #{modifier}
		  , BOARD_TYPE = #{boardType}
		  , CATEGORY   = #{category}
		  , MOD_DT 	   = CURRENT_TIMESTAMP()
	  	WHERE BOARD_NO = #{boardNo}
	</update>
	
	<delete id="deleteBoard" parameterType="Map">
		DELETE FROM hcnc2.HCNC_BOARD
		WHERE BOARD_NO = #{boardNo}
		AND DEL_YN = 'N'
	</delete>
	
	<select id="selectCode" parameterType="Map" resultType="Map">
		SELECT CODE    as code
			 , CODE_NM as codeNm
		  FROM hcnc2.COM_CD_DTL
		 WHERE PT_CODE = #{ptCode}
	</select>
	
	<select id="selectList" parameterType="Map" resultType="Map">
		SELECT BOARD_NO   as boardNo
			 , TITLE	  as title
			 , CONT		  as cont
			 , WRITER	  as writer
			 , MODIFIER	  as modifier
			 , BOARD_TYPE as boardType
			 , CATEGORY	  as category
			 , REG_DT	  as regDt
			 , MOD_DT	  as modDt
		FROM hcnc2.HCNC_BOARD
		WHERE BOARD_NO = #{boardNo}
	</select>

</mapper>